---
name: example-skill
description: >-
  一句话说明这个技能做什么、什么时候触发。description 会被 Agent 用来判断
  是否调用本技能,所以要写清楚"当用户需要 XXX 时使用"。支持单行,也支持
  YAML 折叠块标量(>- 或 |)写多行。
---

# Example Skill

<!--
  这是一个"工作区私有技能"示例。放在 <模板>/.skills/<技能名>/SKILL.md。
  绑定该工作区的会话会自动加载它(由 inno-agent 注入)。
  - 顶部 frontmatter 的 name 必须与目录名一致。
  - 正文写清楚:触发条件、工作流程、产出格式、原则。
  - 需要附带模板/脚本/参考资料时,放在本技能目录下,正文里引用相对路径。
  删掉这些注释后再提交。
-->

把 <输入> 转成 <产出>。

## 触发条件

当用户需要「<场景关键词>」时使用。

## 工作流程

1. <第一步>
2. <第二步>
3. **导出到工作区**:把成果写入当前工作区文件,而不仅停留在对话里。

## 产出格式

```text
<给出一个具体的产出样例,让 Agent 照着产>
```

## 原则

- <原则一>
- <原则二>
